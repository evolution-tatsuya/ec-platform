# A-007 システム設定ページ - E2Eテスト仕様書

## 概要

**ページID**: A-007
**ページ名**: システム設定
**ルート**: `/admin/settings`
**権限レベル**: 管理者のみ
**実装ステータス**: UI実装完了、API未実装

---

## テストケース一覧

### 基本機能テスト
- TC-A007-001: ページ表示確認
- TC-A007-002: 4つのタブ表示確認

### 基本情報タブテスト
- TC-A007-003: 会社情報の表示・編集
- TC-A007-004: 銀行振込先情報の表示・編集
- TC-A007-005: デザイン設定の表示・編集
- TC-A007-006: 画像最適化設定の表示・編集
- TC-A007-007: 基本情報の保存機能

### 決済設定タブテスト
- TC-A007-008: 決済設定タブへの切替
- TC-A007-009: 銀行振込設定
- TC-A007-010: クレジットカード設定
- TC-A007-011: 決済設定の保存

### メール設定タブテスト
- TC-A007-012: メール設定タブへの切替
- TC-A007-013: 送信元メールアドレス設定
- TC-A007-014: 発送通知メールテンプレート編集
- TC-A007-015: メール設定の保存

### 書類テンプレートタブテスト
- TC-A007-016: 書類テンプレートタブへの切替
- TC-A007-017: 3つの書類カード表示
- TC-A007-018: 書類の選択機能
- TC-A007-019: プレビュー機能
- TC-A007-020: 編集機能

### エラーハンドリング・セキュリティテスト
- TC-A007-E001: 未ログイン時のリダイレクト
- TC-A007-E002: 一般ユーザーのアクセス拒否

### 非機能要件テスト
- TC-A007-NF001: レスポンシブ表示（タブレット）
- TC-A007-NF002: レスポンシブ表示（スマートフォン）

---

## テストケース詳細

### TC-A007-001: ページ表示確認

**テスト目的**: 管理者がシステム設定ページにアクセスできることを確認

**前提条件**:
- 管理者アカウントでログイン済み

**テスト手順**:
1. `/admin/settings` にアクセス
2. ページが正常に読み込まれることを確認

**期待結果**:
- ✅ ページタイトル「システム設定」が表示される
- ✅ 4つのタブが表示される（基本情報、決済設定、メール設定、書類テンプレート）
- ✅ デフォルトで「基本情報」タブが選択されている
- ✅ HTTP 200 OK

---

### TC-A007-002: 4つのタブ表示確認

**テスト目的**: 4つのタブが正しく表示され、切り替えできることを確認

**前提条件**:
- TC-A007-001が成功

**テスト手順**:
1. 「基本情報」タブが選択されていることを確認
2. 「決済設定」タブをクリック
3. 「メール設定」タブをクリック
4. 「書類テンプレート」タブをクリック
5. 「基本情報」タブに戻る

**期待結果**:
- ✅ 各タブクリック時に対応するコンテンツが表示される
- ✅ 選択中のタブはハイライト表示される
- ✅ タブ切替時にページ遷移しない（SPA動作）

---

### TC-A007-003: 会社情報の表示・編集

**テスト目的**: 会社情報が正しく表示・編集できることを確認

**前提条件**:
- 「基本情報」タブが表示されている

**テスト手順**:
1. 会社情報セクションを確認
2. 会社名フィールドに「テスト株式会社」を入力
3. 郵便番号フィールドに「100-0001」を入力
4. 住所フィールドに「東京都千代田区千代田1-1」を入力
5. 電話番号フィールドに「03-1234-5678」を入力
6. ロゴ画像をアップロード
7. 「保存」ボタンをクリック

**期待結果**:
- ✅ 各フィールドに入力できる
- ✅ ロゴ画像のプレビューが表示される
- ✅ 「保存」ボタンクリック時にconsole.logが出力される（API未実装）
- ✅ 成功メッセージが表示される

**備考**: 現在はUI実装のみなので、実際のデータ保存は行われない

---

### TC-A007-004: 銀行振込先情報の表示・編集

**テスト目的**: 銀行振込先情報が正しく表示・編集できることを確認

**前提条件**:
- 「基本情報」タブが表示されている

**テスト手順**:
1. 銀行振込先情報セクションを確認
2. 銀行名フィールドに「テスト銀行」を入力
3. 支店名フィールドに「東京支店」を入力
4. 口座種別を「普通」に選択
5. 口座番号フィールドに「1234567」を入力
6. 口座名義フィールドに「テスト カブシキガイシャ」を入力

**期待結果**:
- ✅ 各フィールドに入力できる
- ✅ 口座種別のドロップダウンで「普通」「当座」を選択できる
- ✅ 全フィールドが必須項目として表示される

---

### TC-A007-005: デザイン設定の表示・編集

**テスト目的**: デザイン設定が正しく表示・編集できることを確認

**前提条件**:
- 「基本情報」タブが表示されている

**テスト手順**:
1. デザイン設定セクションを確認
2. プライマリーカラーピッカーをクリック
3. 色を選択（例: #1976d2）
4. セカンダリーカラーピッカーをクリック
5. 色を選択（例: #dc004e）
6. ヒーロー画像をアップロード

**期待結果**:
- ✅ カラーピッカーが表示される
- ✅ 選択した色が反映される
- ✅ ヒーロー画像のプレビューが表示される

---

### TC-A007-006: 画像最適化設定の表示・編集

**テスト目的**: 画像最適化設定が正しく表示・編集できることを確認

**前提条件**:
- 「基本情報」タブが表示されている

**テスト手順**:
1. 画像最適化設定セクションを確認
2. WebP変換スイッチをON/OFF切替
3. 圧縮品質スライダーを60から100の間で調整
4. サムネイルサイズを「小」「中」「大」で切替

**期待結果**:
- ✅ WebP変換スイッチが動作する
- ✅ 圧縮品質スライダーが60-100の範囲で動作する
- ✅ 現在の品質値が表示される（例: 80%）
- ✅ サムネイルサイズのラジオボタンが選択できる

---

### TC-A007-007: 基本情報の保存機能

**テスト目的**: 基本情報の保存機能が動作することを確認

**前提条件**:
- TC-A007-003〜006で各項目を編集済み

**テスト手順**:
1. 「保存」ボタンをクリック
2. 確認ダイアログが表示される場合は「OK」をクリック
3. 保存処理の結果を確認

**期待結果**:
- ✅ console.logに保存データが出力される
- ✅ 成功メッセージが表示される
- ✅ ページがリロードされない

**備考**: API実装後は、実際にデータがバックエンドに保存されることを確認

---

### TC-A007-008: 決済設定タブへの切替

**テスト目的**: 決済設定タブに切り替えできることを確認

**前提条件**:
- システム設定ページが表示されている

**テスト手順**:
1. 「決済設定」タブをクリック
2. 決済設定コンテンツが表示されることを確認

**期待結果**:
- ✅ 決済設定タブがアクティブになる
- ✅ 銀行振込設定セクションが表示される
- ✅ クレジットカード設定セクションが表示される
- ✅ 情報Alertが表示される

---

### TC-A007-009: 銀行振込設定

**テスト目的**: 銀行振込設定が正しく表示・編集できることを確認

**前提条件**:
- 「決済設定」タブが表示されている

**テスト手順**:
1. 銀行振込設定セクションを確認
2. 「銀行振込を有効化」スイッチをON/OFF切替
3. 割引率スライダーを0%から10%の間で調整
4. 「保存」ボタンをクリック

**期待結果**:
- ✅ スイッチがON/OFF切替できる
- ✅ 割引率スライダーが0-10の範囲で動作する
- ✅ 現在の割引率が表示される（例: 3.6%）
- ✅ 保存ボタンクリック時にconsole.logが出力される

---

### TC-A007-010: クレジットカード設定

**テスト目的**: クレジットカード設定が正しく表示・編集できることを確認

**前提条件**:
- 「決済設定」タブが表示されている

**テスト手順**:
1. クレジットカード設定セクションを確認
2. 「クレジットカード決済を有効化」スイッチをON/OFF切替
3. Stripe公開可能キーフィールドに「pk_test_xxxxx」を入力
4. Stripeシークレットキーフィールドに「sk_test_xxxxx」を入力
5. 「保存」ボタンをクリック

**期待結果**:
- ✅ スイッチがON/OFF切替できる
- ✅ APIキーフィールドに入力できる
- ✅ シークレットキーは非表示（type="password"）
- ✅ 保存ボタンクリック時にconsole.logが出力される

---

### TC-A007-011: 決済設定の保存

**テスト目的**: 決済設定の保存機能が動作することを確認

**前提条件**:
- TC-A007-009〜010で各項目を編集済み

**テスト手順**:
1. 「保存」ボタンをクリック
2. 保存処理の結果を確認

**期待結果**:
- ✅ console.logに保存データが出力される
- ✅ 成功メッセージが表示される

**備考**: API実装後は、実際にデータがバックエンドに保存されることを確認

---

### TC-A007-012: メール設定タブへの切替

**テスト目的**: メール設定タブに切り替えできることを確認

**前提条件**:
- システム設定ページが表示されている

**テスト手順**:
1. 「メール設定」タブをクリック
2. メール設定コンテンツが表示されることを確認

**期待結果**:
- ✅ メール設定タブがアクティブになる
- ✅ 送信元メールアドレス設定が表示される
- ✅ 発送通知メールテンプレート設定が表示される

---

### TC-A007-013: 送信元メールアドレス設定

**テスト目的**: 送信元メールアドレスが正しく設定できることを確認

**前提条件**:
- 「メール設定」タブが表示されている

**テスト手順**:
1. 送信元メールアドレスフィールドを確認
2. 「noreply@example.com」を入力
3. 送信者名フィールドに「テストストア」を入力

**期待結果**:
- ✅ メールアドレスフィールドに入力できる
- ✅ 送信者名フィールドに入力できる
- ✅ メールアドレスのバリデーション（形式チェック）

---

### TC-A007-014: 発送通知メールテンプレート編集

**テスト目的**: メールテンプレートが正しく編集できることを確認

**前提条件**:
- 「メール設定」タブが表示されている

**テスト手順**:
1. 発送通知メールテンプレートのテキストエリアを確認
2. テンプレート内容を編集
3. 変数（{customerName}、{orderNumber}等）を確認
4. 「保存」ボタンをクリック

**期待結果**:
- ✅ テキストエリアに12行の入力スペースがある
- ✅ テンプレート変数の説明が表示される
- ✅ 保存ボタンクリック時にconsole.logが出力される

---

### TC-A007-015: メール設定の保存

**テスト目的**: メール設定の保存機能が動作することを確認

**前提条件**:
- TC-A007-013〜014で各項目を編集済み

**テスト手順**:
1. 「保存」ボタンをクリック
2. 保存処理の結果を確認

**期待結果**:
- ✅ console.logに保存データが出力される
- ✅ 成功メッセージが表示される

**備考**: API実装後は、実際にデータがバックエンドに保存されることを確認

---

### TC-A007-016: 書類テンプレートタブへの切替

**テスト目的**: 書類テンプレートタブに切り替えできることを確認

**前提条件**:
- システム設定ページが表示されている

**テスト手順**:
1. 「書類テンプレート」タブをクリック
2. 書類テンプレートコンテンツが表示されることを確認

**期待結果**:
- ✅ 書類テンプレートタブがアクティブになる
- ✅ 3つの書類カード（納品書、領収書、見積書）が表示される
- ✅ pdfme Designer UIのプレースホルダーが表示される

---

### TC-A007-017: 3つの書類カード表示

**テスト目的**: 3つの書類カードが正しく表示されることを確認

**前提条件**:
- 「書類テンプレート」タブが表示されている

**テスト手順**:
1. 納品書カードを確認
2. 領収書カードを確認
3. 見積書カードを確認

**期待結果**:
- ✅ 3つのカードが横並びで表示される
- ✅ 各カードにアイコン、タイトル、説明が表示される
- ✅ 各カードに「プレビュー」「編集」ボタンが表示される

---

### TC-A007-018: 書類の選択機能

**テスト目的**: 書類を選択できることを確認

**前提条件**:
- 「書類テンプレート」タブが表示されている

**テスト手順**:
1. 納品書カードをクリック
2. 領収書カードをクリック
3. 見積書カードをクリック

**期待結果**:
- ✅ クリックした書類カードが選択状態になる（青枠表示）
- ✅ 他のカードは非選択状態になる
- ✅ console.logに選択された書類タイプが出力される

---

### TC-A007-019: プレビュー機能

**テスト目的**: 書類のプレビュー機能が動作することを確認

**前提条件**:
- 「書類テンプレート」タブが表示されている

**テスト手順**:
1. 納品書カードの「プレビュー」ボタンをクリック
2. プレビューの表示を確認
3. プレビューを閉じる

**期待結果**:
- ✅ 「プレビュー」ボタンクリック時にconsole.logが出力される
- ✅ プレビューダイアログまたは新規タブが開く（API実装後）

**備考**: 現在はUI実装のみなので、実際のPDFプレビューは表示されない

---

### TC-A007-020: 編集機能

**テスト目的**: 書類の編集機能が動作することを確認

**前提条件**:
- 「書類テンプレート」タブが表示されている

**テスト手順**:
1. 納品書カードの「編集」ボタンをクリック
2. 編集画面の表示を確認
3. 編集を完了

**期待結果**:
- ✅ 「編集」ボタンクリック時にconsole.logが出力される
- ✅ pdfme Designer UIが表示される（実装後）
- ✅ テンプレートを編集できる（実装後）

**備考**: 現在はプレースホルダーのみ表示。pdfme Designer UIは後のPhaseで実装予定

---

### TC-A007-E001: 未ログイン時のリダイレクト

**テスト目的**: 未ログインユーザーがアクセスできないことを確認

**前提条件**:
- ログアウト状態

**テスト手順**:
1. ブラウザでログアウト
2. `/admin/settings` に直接アクセス

**期待結果**:
- ✅ `/admin/login` にリダイレクトされる
- ✅ ログインページが表示される
- ✅ エラーメッセージ「ログインが必要です」が表示される

---

### TC-A007-E002: 一般ユーザーのアクセス拒否

**テスト目的**: 一般ユーザーが管理画面にアクセスできないことを確認

**前提条件**:
- 一般ユーザーアカウントでログイン済み

**テスト手順**:
1. 一般ユーザーでログイン
2. `/admin/settings` に直接アクセス

**期待結果**:
- ✅ 403 Forbiddenエラーが表示される
- ✅ または `/` （トップページ）にリダイレクトされる
- ✅ エラーメッセージ「管理者権限が必要です」が表示される

---

### TC-A007-NF001: レスポンシブ表示（タブレット）

**テスト目的**: タブレット表示が正しく動作することを確認

**前提条件**:
- システム設定ページが表示されている

**テスト手順**:
1. ブラウザのウィンドウサイズを768×1024（タブレット）に変更
2. 各タブを確認
3. 各設定項目が正しく表示されることを確認

**期待結果**:
- ✅ タブが正しく表示される
- ✅ 設定項目が縦に並ぶレイアウトに変更される
- ✅ 入力フィールドが画面幅に収まる
- ✅ スクロールが正しく動作する

---

### TC-A007-NF002: レスポンシブ表示（スマートフォン）

**テスト目的**: スマートフォン表示が正しく動作することを確認

**前提条件**:
- システム設定ページが表示されている

**テスト手順**:
1. ブラウザのウィンドウサイズを375×667（スマートフォン）に変更
2. 各タブを確認
3. 各設定項目が正しく表示されることを確認

**期待結果**:
- ✅ タブが正しく表示される（必要に応じてスクロール可能）
- ✅ 設定項目が縦に並ぶ
- ✅ 入力フィールドが画面幅に収まる
- ✅ ボタンが押しやすいサイズになっている

---

## テストデータ

### 会社情報テストデータ
```yaml
company_name: 'テスト株式会社'
postal_code: '100-0001'
address: '東京都千代田区千代田1-1'
phone: '03-1234-5678'
```

### 銀行振込先テストデータ
```yaml
bank_name: 'テスト銀行'
branch_name: '東京支店'
account_type: '普通'
account_number: '1234567'
account_holder: 'テスト カブシキガイシャ'
```

### 決済設定テストデータ
```yaml
bank_transfer_enabled: true
bank_transfer_discount_rate: 3.6
credit_card_enabled: true
stripe_public_key: 'pk_test_xxxxx'
stripe_secret_key: 'sk_test_xxxxx'
```

### メール設定テストデータ
```yaml
from_email: 'noreply@example.com'
from_name: 'テストストア'
shipping_notification_template: |
  {customerName} 様

  ご注文いただきました商品を発送いたしました。

  注文番号: {orderNumber}
  追跡番号: {trackingNumber}

  お問い合わせは下記までお願いいたします。
  {supportEmail}
```

---

## Playwrightテストコード例

```typescript
import { test, expect } from '@playwright/test';

test.describe('A-007 システム設定ページ', () => {
  test.beforeEach(async ({ page }) => {
    // 管理者ログイン
    await page.goto('http://localhost:3247/admin/login');
    await page.fill('input[name="email"]', 'admin@ec-platform.local');
    await page.fill('input[name="password"]', 'TestAdmin2025!');
    await page.click('button[type="submit"]');
    await page.waitForURL('http://localhost:3247/admin');
  });

  test('TC-A007-001: ページ表示確認', async ({ page }) => {
    await page.goto('http://localhost:3247/admin/settings');
    await expect(page).toHaveTitle(/システム設定/);
    await expect(page.locator('h4')).toContainText('システム設定');
    await expect(page.locator('[role="tab"]')).toHaveCount(4);
  });

  test('TC-A007-003: 会社情報の表示・編集', async ({ page }) => {
    await page.goto('http://localhost:3247/admin/settings');
    await page.fill('input[name="companyName"]', 'テスト株式会社');
    await page.fill('input[name="postalCode"]', '100-0001');
    await page.fill('input[name="address"]', '東京都千代田区千代田1-1');
    await page.fill('input[name="phone"]', '03-1234-5678');
    await page.click('button:has-text("保存")');
    await expect(page.locator('.MuiAlert-message')).toContainText('保存しました');
  });
});
```

---

## 注意事項

1. **現在の実装状況**: UI実装完了、API未実装
2. **保存機能**: 現在はconsole.logのみ、実際の保存はAPI実装後
3. **pdfme Designer UI**: プレースホルダーのみ、実装は後のPhase
4. **Cloudflare R2連携**: 画像アップロード機能はAPI実装後

---

**作成日**: 2026-04-16
**最終更新**: 2026-04-16
**テストケース総数**: 24件
