# A-005: 顧客管理ページ E2Eテスト仕様書

## 概要

- **ページ名**: 顧客管理ページ
- **ページID**: A-005
- **ルート**: `/admin/customers`
- **権限**: 管理者のみ
- **レイアウト**: MainLayout（管理画面用）
- **作成日**: 2026-04-15
- **最終更新**: 2026-04-15

---

## テスト環境

- **URL**: `http://localhost:3247/admin/customers`
- **前提条件**:
  - 管理者としてログイン済み（`admin@ec-platform.local` / `TestAdmin2025!`）
  - テストデータ: ダミー顧客5名
  - データベース: `.env.local`のDATABASE_URL接続
  - バックエンドサーバー: `http://localhost:8432`（起動済み）

---

## テストケース一覧

### 基本機能

#### TC-A005-001: ページ表示確認（管理者ログイン後）

**テスト目的**
管理者がログイン後、顧客管理ページに正常にアクセスできることを確認する。

**前提条件**
- 管理者としてログイン済み

**テスト手順**
1. ブラウザで`/admin/customers`にアクセス
2. ページが読み込まれるまで待機

**期待結果**
- ページタイトル「顧客管理」が表示される
- Headerコンポーネントが表示される
- 検索・絞り込みカードが表示される
- DataGridに顧客一覧が表示される

**備考**
- ログインしていない場合はログインページへリダイレクトされる

---

#### TC-A005-002: 顧客一覧の表示確認（DataGrid）

**テスト目的**
DataGridに顧客データが正しく表示されることを確認する。

**前提条件**
- 顧客管理ページを表示済み
- ダミーデータ5名が存在

**テスト手順**
1. DataGridを確認
2. 各列のヘッダーを確認
3. データ行の表示内容を確認

**期待結果**
- 以下のカラムが表示される:
  - 顧客名
  - メールアドレス
  - 登録日（yyyy/MM/dd形式）
  - 購入回数（「XX回」形式）
  - 累計購入金額（「¥XXX,XXX」形式、カンマ区切り）
  - タグ（Chip形式）
  - 操作（詳細ボタン）
- デフォルトソート: 累計購入金額の降順
- ページネーション: 10件/ページ（変更可能: 10, 25, 50）
- ダミー顧客5名すべてが表示される

**備考**
- 初期表示時は「すべて」のフィルター状態

---

#### TC-A005-003: 検索機能（名前・メール）

**テスト目的**
検索ボックスで顧客名またはメールアドレスで絞り込めることを確認する。

**前提条件**
- 顧客管理ページを表示済み

**テスト手順**
1. 検索ボックスに「山田」と入力
2. DataGridの表示を確認
3. 検索ボックスをクリアし、「yamada@example.com」と入力
4. DataGridの表示を確認
5. 検索ボックスをクリア
6. DataGridの表示を確認

**期待結果**
- ステップ2: 「山田太郎」のみ表示される
- ステップ4: 「山田太郎」のみ表示される
- ステップ6: すべての顧客（5名）が表示される

**備考**
- 部分一致検索（大文字小文字区別なし）

---

#### TC-A005-004: 購入回数フィルター（すべて/1回/2〜5回/6〜10回/11回以上）

**テスト目的**
購入回数で顧客を絞り込めることを確認する。

**前提条件**
- 顧客管理ページを表示済み
- ダミーデータの購入回数:
  - 山田太郎: 12回
  - 佐藤花子: 5回
  - 田中一郎: 1回
  - 鈴木美咲: 25回
  - 高橋健太: 8回

**テスト手順**
1. 購入回数セレクトボックスで「1回」を選択
2. DataGridを確認
3. 「2〜5回」を選択
4. DataGridを確認
5. 「6〜10回」を選択
6. DataGridを確認
7. 「11回以上」を選択
8. DataGridを確認
9. 「すべて」を選択
10. DataGridを確認

**期待結果**
- ステップ2: 田中一郎（1回）のみ表示
- ステップ4: 佐藤花子（5回）のみ表示
- ステップ6: 高橋健太（8回）のみ表示
- ステップ8: 山田太郎（12回）、鈴木美咲（25回）が表示
- ステップ10: すべての顧客（5名）が表示

**備考**
- フィルターは検索と組み合わせ可能

---

### 顧客詳細

#### TC-A005-005: 顧客詳細ダイアログの表示

**テスト目的**
「詳細」ボタンをクリックして顧客詳細ダイアログが正しく開くことを確認する。

**前提条件**
- 顧客管理ページを表示済み

**テスト手順**
1. DataGridの1行目（山田太郎）の「詳細」ボタンをクリック
2. ダイアログが表示されることを確認

**期待結果**
- ダイアログのタイトル「顧客詳細」が表示される
- ダイアログ内に以下のセクションが表示される:
  - 基本情報
  - 統計情報
  - タグ編集
  - 購入履歴（最近5件）
- 「キャンセル」ボタンと「タグを保存」ボタンが表示される

**備考**
- ダイアログ外をクリックしても閉じない（明示的なクローズが必要）

---

#### TC-A005-006: 基本情報の表示確認

**テスト目的**
顧客詳細ダイアログの基本情報セクションが正しく表示されることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み

**テスト手順**
1. 基本情報セクションを確認

**期待結果**
- 以下の項目が表示される:
  - **氏名**: 山田太郎
  - **メール**: yamada@example.com
  - **電話番号**: 090-1234-5678
  - **登録日**: 2025年1月15日（`yyyy年M月d日`形式）

**備考**
- 日付フォーマットは date-fns の ja ロケール使用

---

#### TC-A005-007: 統計情報の表示確認（カード4つ）

**テスト目的**
顧客詳細ダイアログの統計情報カードが正しく表示されることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み

**テスト手順**
1. 統計情報セクションのカードを確認

**期待結果**
- 以下の4つのカードが表示される:
  1. **累計購入金額**: ¥180,000（カンマ区切り）
  2. **購入回数**: 12回
  3. **最終購入日**: 2026/04/10（`yyyy/MM/dd`形式）
  4. **リピート率**: 83%（小数点以下切り捨て）

**備考**
- リピート率 = `(repeatRate * 100).toFixed(0)`（0.83 → 83%）

---

#### TC-A005-008: 購入履歴の表示

**テスト目的**
顧客詳細ダイアログの購入履歴テーブルが正しく表示されることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み

**テスト手順**
1. 購入履歴（最近5件）セクションのテーブルを確認

**期待結果**
- テーブルヘッダー: 注文番号 | 注文日 | 金額 | ステータス
- 山田太郎の注文3件が表示される:
  1. ORD-20260410-001 | 2026/04/10 | ¥15,800 | 発送準備中（primaryカラーChip）
  2. ORD-20260315-002 | 2026/03/15 | ¥28,000 | 配達完了（successカラーChip）
  3. ORD-20260210-003 | 2026/02/10 | ¥42,500 | 配達完了（successカラーChip）
- 注文番号は monospace フォント
- 金額はカンマ区切り
- ステータスは日本語表示でChip形式

**備考**
- 注文が5件超の場合も最大5件まで表示

---

#### TC-A005-009: タグ編集機能（Autocomplete）

**テスト目的**
タグ編集用のAutocompleteコンポーネントが正常に動作することを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み

**テスト手順**
1. タグ編集セクションのAutocompleteを確認
2. 初期状態のタグを確認

**期待結果**
- Autocomplete入力欄が表示される
- プレースホルダー: 「タグを選択または追加」
- 初期タグ: 「VIP」「リピーター」（primaryカラーChip）
- タグの削除ボタン（×）が表示される

**備考**
- Autocomplete は multiple モード、freeSolo 有効

---

#### TC-A005-010: タグの追加（新規タグ含む）

**テスト目的**
既存タグと新規タグをAutocompleteで追加できることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み
- 初期タグ: VIP、リピーター

**テスト手順**
1. Autocomplete入力欄をクリック
2. ドロップダウンから「高額購入者」を選択
3. 入力欄に「優良顧客」と入力してEnterキーを押す（新規タグ）
4. タグの表示を確認

**期待結果**
- タグが4つ表示される: VIP、リピーター、高額購入者、優良顧客
- すべてprimaryカラーのChip形式
- 削除ボタン（×）が表示される

**備考**
- 既存タグ選択肢: VIP、リピーター、高額購入者、要注意

---

#### TC-A005-011: タグの削除

**テスト目的**
タグの削除ボタンでタグを削除できることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み
- タグ: VIP、リピーター

**テスト手順**
1. 「VIP」タグの削除ボタン（×）をクリック
2. タグの表示を確認

**期待結果**
- 「VIP」タグが削除される
- 残りのタグ: リピーター

**備考**
- 削除は即座に反映される（保存前）

---

#### TC-A005-012: タグの保存

**テスト目的**
「タグを保存」ボタンでタグ編集が保存されることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み
- タグを編集済み（例: リピーター、高額購入者）

**テスト手順**
1. 「タグを保存」ボタンをクリック
2. ダイアログの状態を確認
3. コンソールログを確認

**期待結果**
- ダイアログが閉じる
- コンソールに以下のログが出力される:
  ```
  タグ保存: { customerId: '1', tags: ['リピーター', '高額購入者'] }
  ```
- DataGridの山田太郎の行のタグが更新される

**備考**
- 実際のAPI接続は後のフェーズで実装予定
- 現状はコンソールログのみ

---

### データ表示

#### TC-A005-013: 累計購入金額の表示形式（カンマ区切り）

**テスト目的**
累計購入金額が正しくフォーマットされて表示されることを確認する。

**前提条件**
- 顧客管理ページを表示済み

**テスト手順**
1. DataGridの累計購入金額列を確認
2. 顧客詳細ダイアログの累計購入金額カードを確認

**期待結果**
- DataGrid:
  - 山田太郎: ¥180,000
  - 佐藤花子: ¥68,000
  - 田中一郎: ¥42,500
  - 鈴木美咲: ¥520,000
  - 高橋健太: ¥125,000
- 顧客詳細ダイアログ: ¥180,000（山田太郎の場合）
- すべてカンマ区切り、太字表示

**備考**
- `toLocaleString()`メソッド使用

---

#### TC-A005-014: リピート率の表示形式（パーセント）

**テスト目的**
リピート率が正しくパーセント形式で表示されることを確認する。

**前提条件**
- 山田太郎の顧客詳細ダイアログを表示済み

**テスト手順**
1. 統計情報のリピート率カードを確認

**期待結果**
- リピート率: 83%
- 小数点以下は切り捨て
- 太字表示

**備考**
- 計算式: `(repeatRate * 100).toFixed(0)` → 0.83 → 83%

---

#### TC-A005-015: 日付フォーマットの確認

**テスト目的**
日付が正しいフォーマットで表示されることを確認する。

**前提条件**
- 顧客管理ページを表示済み
- 顧客詳細ダイアログを表示済み

**テスト手順**
1. DataGridの登録日列を確認（山田太郎: 2025-01-15）
2. 顧客詳細ダイアログの基本情報→登録日を確認
3. 顧客詳細ダイアログの統計情報→最終購入日を確認
4. 購入履歴の注文日を確認

**期待結果**
- DataGrid登録日: `2025/01/15`（`yyyy/MM/dd`形式）
- 基本情報登録日: `2025年1月15日`（`yyyy年M月d日`形式）
- 統計情報最終購入日: `2026/04/10`（`yyyy/MM/dd`形式）
- 購入履歴注文日: `2026/04/10`（`yyyy/MM/dd`形式）

**備考**
- date-fns の `format()` 関数、ja ロケール使用

---

#### TC-A005-016: タグChipの表示

**テスト目的**
タグがChip形式で正しく表示されることを確認する。

**前提条件**
- 顧客管理ページを表示済み

**テスト手順**
1. DataGridのタグ列を確認（山田太郎: VIP、リピーター）
2. 顧客詳細ダイアログのタグ編集Autocompleteを確認

**期待結果**
- DataGrid:
  - タグがChip形式で横並び表示（spacing: 0.5）
  - サイズ: small
  - カラー: primary
- 顧客詳細ダイアログ:
  - Autocomplete内のタグがChip形式
  - カラー: primary
  - 削除ボタン（×）付き

**備考**
- タグが0個の場合は空欄表示（田中一郎）

---

## エラーケース

#### TC-A005-E001: 未ログイン時のアクセス

**テスト目的**
未ログイン状態でのアクセスが正しくリダイレクトされることを確認する。

**前提条件**
- ログアウト済み

**テスト手順**
1. ブラウザで`/admin/customers`に直接アクセス

**期待結果**
- ログインページ（`/admin/login`）にリダイレクトされる
- 顧客管理ページは表示されない

**備考**
- ProtectedRouteコンポーネントによる保護

---

#### TC-A005-E002: 一般ユーザーでのアクセス

**テスト目的**
一般ユーザー（管理者以外）でのアクセスが拒否されることを確認する。

**前提条件**
- 一般ユーザーとしてログイン済み

**テスト手順**
1. ブラウザで`/admin/customers`に直接アクセス

**期待結果**
- アクセス拒否またはトップページにリダイレクトされる
- 顧客管理ページは表示されない

**備考**
- 管理者権限チェック必須

---

## 非機能要件テスト

#### TC-A005-NF001: レスポンシブ表示（スマホ）

**テスト目的**
スマホサイズでのレスポンシブ表示を確認する。

**前提条件**
- 顧客管理ページを表示済み
- ブラウザをスマホサイズ（375×667px）にリサイズ

**テスト手順**
1. 検索・絞り込みカードの表示を確認
2. DataGridの表示を確認
3. 顧客詳細ダイアログを開く
4. ダイアログの表示を確認

**期待結果**
- 検索ボックスと購入回数セレクトが縦並びで表示される（Grid xs=12）
- DataGridが横スクロール可能
- ダイアログが画面幅に収まる（maxWidth: md）
- 統計情報カードが2列×2行で表示される（Grid xs=6）

**備考**
- MUIのBreakpoint使用

---

#### TC-A005-NF002: ページネーション動作

**テスト目的**
DataGridのページネーションが正常に動作することを確認する。

**前提条件**
- ダミーデータ20名以上（現状は5名なので追加が必要）
- 顧客管理ページを表示済み

**テスト手順**
1. DataGridの下部のページネーションを確認
2. 「次のページ」ボタンをクリック
3. ページサイズを25に変更
4. ページサイズを10に戻す

**期待結果**
- ステップ2: 2ページ目が表示される（11〜20件目）
- ステップ3: 1ページに25件表示される
- ステップ4: 1ページに10件表示される

**備考**
- ページサイズ選択肢: 10, 25, 50

---

#### TC-A005-NF003: ソート機能

**テスト目的**
DataGridの各列でソートができることを確認する。

**前提条件**
- 顧客管理ページを表示済み

**テスト手順**
1. 累計購入金額列のヘッダーをクリック（昇順）
2. 累計購入金額列のヘッダーを再度クリック（降順）
3. 購入回数列のヘッダーをクリック（昇順）
4. 登録日列のヘッダーをクリック（降順）

**期待結果**
- ステップ1: 田中一郎（¥42,500）が最上位
- ステップ2: 鈴木美咲（¥520,000）が最上位（デフォルト）
- ステップ3: 田中一郎（1回）が最上位
- ステップ4: 田中一郎（2026-04-01登録）が最上位

**備考**
- タグ列と操作列はソート不可（sortable: false）

---

## テストデータ

### ダミー顧客データ（5名）

| ID | 氏名 | メール | 電話番号 | 登録日 | 購入回数 | 累計購入金額 | 最終購入日 | リピート率 | タグ |
|----|------|--------|----------|--------|----------|--------------|------------|-----------|------|
| 1 | 山田太郎 | yamada@example.com | 090-1234-5678 | 2025-01-15 | 12 | ¥180,000 | 2026-04-10 | 83% | VIP, リピーター |
| 2 | 佐藤花子 | sato@example.com | 080-9876-5432 | 2025-03-20 | 5 | ¥68,000 | 2026-04-12 | 60% | リピーター |
| 3 | 田中一郎 | tanaka@example.com | 070-1111-2222 | 2026-04-01 | 1 | ¥42,500 | 2026-04-14 | 0% | - |
| 4 | 鈴木美咲 | suzuki@example.com | 090-3333-4444 | 2024-11-10 | 25 | ¥520,000 | 2026-04-08 | 92% | VIP, リピーター, 高額購入者 |
| 5 | 高橋健太 | takahashi@example.com | 080-5555-6666 | 2025-06-18 | 8 | ¥125,000 | 2026-03-28 | 75% | リピーター |

### 既存タグ選択肢

- VIP
- リピーター
- 高額購入者
- 要注意

---

## 備考

- **実装状況**: UI実装完了（ダミーデータ使用）
- **API実装**: 未実装（Phase 10以降で実装予定）
- **コンソールログ**: タグ保存時にログ出力（`console.log('タグ保存:', { customerId, tags })`）
- **使用コンポーネント**: DataGrid（MUI X）, Autocomplete, Chip, Dialog, TextField, Select, Table
- **日付ライブラリ**: date-fns（jaロケール使用）

---

## 自動テストスクリプト例（Playwright）

```typescript
// tests/e2e/admin-customers.spec.ts

import { test, expect } from '@playwright/test';

test.describe('A-005: 顧客管理ページ', () => {
  test.beforeEach(async ({ page }) => {
    // 管理者ログイン
    await page.goto('http://localhost:3247/admin/login');
    await page.fill('input[type="email"]', 'admin@ec-platform.local');
    await page.fill('input[type="password"]', 'TestAdmin2025!');
    await page.click('button[type="submit"]');
    await page.waitForURL('**/admin/dashboard');

    // 顧客管理ページへ移動
    await page.goto('http://localhost:3247/admin/customers');
    await page.waitForSelector('h4:has-text("顧客管理")');
  });

  test('TC-A005-001: ページ表示確認', async ({ page }) => {
    await expect(page.locator('h4')).toHaveText('顧客管理');
    await expect(page.locator('input[placeholder*="名前またはメール"]')).toBeVisible();
    await expect(page.locator('.MuiDataGrid-root')).toBeVisible();
  });

  test('TC-A005-003: 検索機能（名前）', async ({ page }) => {
    await page.fill('input[placeholder*="名前またはメール"]', '山田');
    await expect(page.locator('.MuiDataGrid-row')).toHaveCount(1);
    await expect(page.locator('.MuiDataGrid-row').first()).toContainText('山田太郎');
  });

  test('TC-A005-004: 購入回数フィルター（1回）', async ({ page }) => {
    await page.click('label:has-text("購入回数")');
    await page.click('li:has-text("1回")');
    await expect(page.locator('.MuiDataGrid-row')).toHaveCount(1);
    await expect(page.locator('.MuiDataGrid-row').first()).toContainText('田中一郎');
  });

  test('TC-A005-005: 顧客詳細ダイアログ表示', async ({ page }) => {
    await page.click('button:has-text("詳細")').first();
    await expect(page.locator('h2:has-text("顧客詳細")')).toBeVisible();
    await expect(page.locator('text=基本情報')).toBeVisible();
    await expect(page.locator('text=統計情報')).toBeVisible();
    await expect(page.locator('text=タグ編集')).toBeVisible();
    await expect(page.locator('text=購入履歴（最近5件）')).toBeVisible();
  });

  test('TC-A005-010: タグの追加', async ({ page }) => {
    await page.click('button:has-text("詳細")').first();
    await page.click('.MuiAutocomplete-root input');
    await page.click('li:has-text("高額購入者")');
    await expect(page.locator('.MuiChip-label:has-text("高額購入者")')).toBeVisible();
  });

  test('TC-A005-012: タグの保存', async ({ page }) => {
    await page.click('button:has-text("詳細")').first();
    await page.click('.MuiAutocomplete-root input');
    await page.click('li:has-text("高額購入者")');
    await page.click('button:has-text("タグを保存")');
    await expect(page.locator('h2:has-text("顧客詳細")')).not.toBeVisible();
  });

  test('TC-A005-013: 累計購入金額の表示形式', async ({ page }) => {
    const cell = page.locator('.MuiDataGrid-row').first().locator('text=/¥[0-9,]+/');
    await expect(cell).toHaveText('¥180,000');
  });

  test('TC-A005-NF003: ソート機能（累計購入金額）', async ({ page }) => {
    await page.click('[data-field="totalSpent"] .MuiDataGrid-columnHeaderTitle');
    const firstRow = page.locator('.MuiDataGrid-row').first();
    await expect(firstRow).toContainText('田中一郎');
    await expect(firstRow).toContainText('¥42,500');
  });
});
```

---

## 更新履歴

| 日付 | バージョン | 更新者 | 更新内容 |
|------|------------|--------|----------|
| 2026-04-15 | 1.0.0 | Claude | 初版作成 |
