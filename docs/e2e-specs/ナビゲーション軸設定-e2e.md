# A-008 ナビゲーション軸設定ページ - E2Eテスト仕様書

## 概要

**ページID**: A-008
**ページ名**: ナビゲーション軸設定
**ルート**: `/admin/navigation`
**権限レベル**: 管理者のみ
**実装ステータス**: UI実装完了、API未実装

---

## テストケース一覧

### 基本機能テスト
- TC-A008-001: ページ表示確認
- TC-A008-002: カテゴリー選択機能

### 軸一覧表示テスト
- TC-A008-003: 軸一覧の表示確認（DataGrid）
- TC-A008-004: カテゴリー別軸の表示

### 軸追加テスト
- TC-A008-005: 軸追加ダイアログの表示
- TC-A008-006: 軸追加フォームの入力
- TC-A008-007: 軸の追加実行

### 軸編集テスト
- TC-A008-008: 軸編集ダイアログの表示
- TC-A008-009: 軸情報の編集
- TC-A008-010: 選択肢の追加・削除

### 軸削除テスト
- TC-A008-011: 軸削除の実行
- TC-A008-012: 削除確認ダイアログ

### 軸順序変更テスト
- TC-A008-013: 上へ移動ボタン
- TC-A008-014: 下へ移動ボタン

### プレビュー機能テスト
- TC-A008-015: プレビューエリアの表示

### エラーハンドリング・セキュリティテスト
- TC-A008-E001: 未ログイン時のリダイレクト
- TC-A008-E002: 一般ユーザーのアクセス拒否
- TC-A008-E003: 必須項目のバリデーション

### 非機能要件テスト
- TC-A008-NF001: レスポンシブ表示（タブレット）
- TC-A008-NF002: レスポンシブ表示（スマートフォン）

---

## テストケース詳細

### TC-A008-001: ページ表示確認

**テスト目的**: 管理者がナビゲーション軸設定ページにアクセスできることを確認

**前提条件**:
- 管理者アカウントでログイン済み

**テスト手順**:
1. `/admin/navigation` にアクセス
2. ページが正常に読み込まれることを確認

**期待結果**:
- ✅ ページタイトル「ナビゲーション軸設定」が表示される
- ✅ カテゴリー選択（Select）が表示される
- ✅ 軸一覧（DataGrid）が表示される
- ✅ 「軸を追加」ボタンが表示される
- ✅ プレビューエリアが表示される
- ✅ HTTP 200 OK

---

### TC-A008-002: カテゴリー選択機能

**テスト目的**: カテゴリー選択が正しく動作することを確認

**前提条件**:
- TC-A008-001が成功

**テスト手順**:
1. カテゴリー選択（Select）を確認
2. 「車パーツ」を選択
3. 「イベント」を選択
4. 「デジタル」を選択

**期待結果**:
- ✅ 3つのカテゴリー（車パーツ、イベント、デジタル）が選択肢に表示される
- ✅ カテゴリー選択時に対応する軸一覧が表示される
- ✅ デフォルトで「車パーツ」が選択されている

---

### TC-A008-003: 軸一覧の表示確認（DataGrid）

**テスト目的**: 軸一覧が正しく表示されることを確認

**前提条件**:
- 「車パーツ」カテゴリーが選択されている

**テスト手順**:
1. DataGridを確認
2. 列を確認（軸名、軸キー、軸種別、表示形式、選択肢数、順序、操作）
3. データ行を確認

**期待結果**:
- ✅ DataGridが表示される
- ✅ 7列（軸名、軸キー、軸種別、表示形式、選択肢数、順序、操作）が表示される
- ✅ ダミーデータ（メーカー、車種、パーツカテゴリー）が表示される
- ✅ 順序ボタン（↑↓）が表示される
- ✅ 編集・削除ボタンが表示される

---

### TC-A008-004: カテゴリー別軸の表示

**テスト目的**: カテゴリー別に軸が正しく表示されることを確認

**前提条件**:
- TC-A008-002が成功

**テスト手順**:
1. 「車パーツ」を選択 → 軸一覧を確認
2. 「イベント」を選択 → 軸一覧を確認
3. 「デジタル」を選択 → 軸一覧を確認

**期待結果**:
- ✅ 車パーツ: 3軸（メーカー、車種、パーツカテゴリー）が表示される
- ✅ イベント: 1軸（イベント種類）が表示される
- ✅ デジタル: 1軸（ジャンル）が表示される
- ✅ カテゴリー切替時にDataGridが更新される

---

### TC-A008-005: 軸追加ダイアログの表示

**テスト目的**: 軸追加ダイアログが正しく表示されることを確認

**前提条件**:
- ナビゲーション軸設定ページが表示されている

**テスト手順**:
1. 「軸を追加」ボタンをクリック
2. ダイアログが表示されることを確認

**期待結果**:
- ✅ 軸追加ダイアログが表示される
- ✅ ダイアログタイトル「ナビゲーション軸を追加」が表示される
- ✅ 以下のフィールドが表示される:
  - 軸名（TextField）
  - 軸キー（TextField）
  - 軸種別（Select）
  - 表示形式（Select）
  - アイコン（TextField）
  - 選択肢（テキストエリア）
  - 親軸（Select、任意）
- ✅ 「キャンセル」「保存」ボタンが表示される

---

### TC-A008-006: 軸追加フォームの入力

**テスト目的**: 軸追加フォームに正しく入力できることを確認

**前提条件**:
- TC-A008-005が成功

**テスト手順**:
1. 軸名フィールドに「排気量」を入力
2. 軸キーフィールドに「displacement」を入力
3. 軸種別で「選択式」を選択
4. 表示形式で「ドロップダウン」を選択
5. アイコンフィールドに「🚗」を入力
6. 選択肢に以下を入力:
   ```
   軽自動車（660cc）
   1000cc〜1500cc
   1500cc〜2000cc
   2000cc以上
   ```
7. 親軸で「車種」を選択（任意）

**期待結果**:
- ✅ 各フィールドに入力できる
- ✅ 軸種別の選択肢: 選択式、複数選択、検索、範囲
- ✅ 表示形式の選択肢: ドロップダウン、ボタン、ラジオ
- ✅ 親軸は任意項目
- ✅ 選択肢は1行に1つ入力できる

---

### TC-A008-007: 軸の追加実行

**テスト目的**: 軸の追加が正しく実行されることを確認

**前提条件**:
- TC-A008-006で各項目を入力済み

**テスト手順**:
1. 「保存」ボタンをクリック
2. 保存処理の結果を確認

**期待結果**:
- ✅ console.logに保存データが出力される
- ✅ ダイアログが閉じる
- ✅ 成功メッセージが表示される
- ✅ 軸一覧に新しい軸が追加される（API実装後）

**備考**: 現在はUI実装のみなので、実際のデータ保存は行われない

---

### TC-A008-008: 軸編集ダイアログの表示

**テスト目的**: 軸編集ダイアログが正しく表示されることを確認

**前提条件**:
- 軸一覧が表示されている

**テスト手順**:
1. 任意の軸の「編集」ボタンをクリック
2. 編集ダイアログが表示されることを確認

**期待結果**:
- ✅ 軸編集ダイアログが表示される
- ✅ ダイアログタイトル「ナビゲーション軸を編集」が表示される
- ✅ 既存の軸情報がフィールドに表示される
- ✅ すべてのフィールドが編集可能

---

### TC-A008-009: 軸情報の編集

**テスト目的**: 軸情報を編集できることを確認

**前提条件**:
- TC-A008-008が成功

**テスト手順**:
1. 軸名を変更
2. 軸種別を変更
3. 表示形式を変更
4. アイコンを変更
5. 「保存」ボタンをクリック

**期待結果**:
- ✅ 各フィールドを編集できる
- ✅ console.logに更新データが出力される
- ✅ ダイアログが閉じる
- ✅ 成功メッセージが表示される
- ✅ 軸一覧に変更が反映される（API実装後）

---

### TC-A008-010: 選択肢の追加・削除

**テスト目的**: 選択肢を追加・削除できることを確認

**前提条件**:
- 軸編集ダイアログが表示されている

**テスト手順**:
1. 選択肢テキストエリアを確認
2. 新しい選択肢を追加（新しい行を入力）
3. 既存の選択肢を削除（行を削除）
4. 「保存」ボタンをクリック

**期待結果**:
- ✅ 選択肢テキストエリアに複数行入力できる
- ✅ 選択肢を自由に追加・削除できる
- ✅ 保存時に選択肢が更新される（API実装後）

---

### TC-A008-011: 軸削除の実行

**テスト目的**: 軸を削除できることを確認

**前提条件**:
- 軸一覧が表示されている

**テスト手順**:
1. 任意の軸の「削除」ボタンをクリック
2. 削除処理の結果を確認

**期待結果**:
- ✅ 「削除」ボタンクリック時にconsole.logが出力される
- ✅ 削除確認ダイアログが表示される（API実装後）
- ✅ 削除が実行される（API実装後）
- ✅ 軸一覧から削除される（API実装後）

**備考**: 現在はUI実装のみなので、実際の削除は行われない

---

### TC-A008-012: 削除確認ダイアログ

**テスト目的**: 削除確認ダイアログが表示されることを確認

**前提条件**:
- TC-A008-011が成功（API実装後）

**テスト手順**:
1. 軸の「削除」ボタンをクリック
2. 削除確認ダイアログを確認
3. 「キャンセル」をクリック
4. 再度「削除」ボタンをクリック
5. 「削除」を確認

**期待結果**:
- ✅ 削除確認ダイアログが表示される
- ✅ 警告メッセージ「この軸を削除すると、関連する商品データが影響を受ける可能性があります」が表示される
- ✅ 「キャンセル」で削除がキャンセルされる
- ✅ 「削除」で削除が実行される

**備考**: API実装後に確認

---

### TC-A008-013: 上へ移動ボタン

**テスト目的**: 軸の順序を上に移動できることを確認

**前提条件**:
- 軸一覧に複数の軸が表示されている

**テスト手順**:
1. 2番目の軸の「↑」ボタンをクリック
2. 順序の変更を確認

**期待結果**:
- ✅ 「↑」ボタンクリック時にconsole.logが出力される
- ✅ 軸の順序が1つ上に移動する（API実装後）
- ✅ 1番目の軸の「↑」ボタンは無効化される

**備考**: 現在はUI実装のみなので、実際の順序変更は行われない

---

### TC-A008-014: 下へ移動ボタン

**テスト目的**: 軸の順序を下に移動できることを確認

**前提条件**:
- 軸一覧に複数の軸が表示されている

**テスト手順**:
1. 1番目の軸の「↓」ボタンをクリック
2. 順序の変更を確認

**期待結果**:
- ✅ 「↓」ボタンクリック時にconsole.logが出力される
- ✅ 軸の順序が1つ下に移動する（API実装後）
- ✅ 最後の軸の「↓」ボタンは無効化される

**備考**: 現在はUI実装のみなので、実際の順序変更は行われない

---

### TC-A008-015: プレビューエリアの表示

**テスト目的**: プレビューエリアが正しく表示されることを確認

**前提条件**:
- ナビゲーション軸設定ページが表示されている

**テスト手順**:
1. プレビューエリアを確認
2. カテゴリーを切り替え
3. プレビューの更新を確認

**期待結果**:
- ✅ プレビューエリアが表示される
- ✅ プレースホルダーテキスト「プレビュー機能は今後実装予定です」が表示される
- ✅ カテゴリー切替時にプレビューが更新される（実装後）

**備考**: プレビュー機能は後のPhaseで実装予定

---

### TC-A008-E001: 未ログイン時のリダイレクト

**テスト目的**: 未ログインユーザーがアクセスできないことを確認

**前提条件**:
- ログアウト状態

**テスト手順**:
1. ブラウザでログアウト
2. `/admin/navigation` に直接アクセス

**期待結果**:
- ✅ `/admin/login` にリダイレクトされる
- ✅ ログインページが表示される
- ✅ エラーメッセージ「ログインが必要です」が表示される

---

### TC-A008-E002: 一般ユーザーのアクセス拒否

**テスト目的**: 一般ユーザーが管理画面にアクセスできないことを確認

**前提条件**:
- 一般ユーザーアカウントでログイン済み

**テスト手順**:
1. 一般ユーザーでログイン
2. `/admin/navigation` に直接アクセス

**期待結果**:
- ✅ 403 Forbiddenエラーが表示される
- ✅ または `/` （トップページ）にリダイレクトされる
- ✅ エラーメッセージ「管理者権限が必要です」が表示される

---

### TC-A008-E003: 必須項目のバリデーション

**テスト目的**: 必須項目のバリデーションが動作することを確認

**前提条件**:
- 軸追加ダイアログが表示されている

**テスト手順**:
1. 軸名を入力せずに「保存」をクリック
2. エラーメッセージを確認
3. 軸キーを入力せずに「保存」をクリック
4. エラーメッセージを確認

**期待結果**:
- ✅ 軸名が未入力の場合、エラーメッセージ「軸名は必須です」が表示される
- ✅ 軸キーが未入力の場合、エラーメッセージ「軸キーは必須です」が表示される
- ✅ 必須項目がすべて入力されるまで保存できない

**備考**: バリデーションはAPI実装時に追加

---

### TC-A008-NF001: レスポンシブ表示（タブレット）

**テスト目的**: タブレット表示が正しく動作することを確認

**前提条件**:
- ナビゲーション軸設定ページが表示されている

**テスト手順**:
1. ブラウザのウィンドウサイズを768×1024（タブレット）に変更
2. 各要素の表示を確認

**期待結果**:
- ✅ カテゴリー選択が正しく表示される
- ✅ DataGridが画面幅に収まる
- ✅ 順序ボタン、操作ボタンが押しやすいサイズ
- ✅ プレビューエリアが適切に配置される
- ✅ スクロールが正しく動作する

---

### TC-A008-NF002: レスポンシブ表示（スマートフォン）

**テスト目的**: スマートフォン表示が正しく動作することを確認

**前提条件**:
- ナビゲーション軸設定ページが表示されている

**テスト手順**:
1. ブラウザのウィンドウサイズを375×667（スマートフォン）に変更
2. 各要素の表示を確認

**期待結果**:
- ✅ カテゴリー選択が正しく表示される
- ✅ DataGridが横スクロール可能
- ✅ ボタンが押しやすいサイズ
- ✅ ダイアログがモバイル対応

---

## テストデータ

### 車パーツカテゴリーの軸
```yaml
- axisName: 'メーカー'
  axisKey: 'maker'
  axisType: 'SELECT'
  displayType: 'DROPDOWN'
  icon: '🚗'
  options: ['トヨタ', 'ホンダ', '日産', 'マツダ', 'スバル']
  order: 1

- axisName: '車種'
  axisKey: 'model'
  axisType: 'SELECT'
  displayType: 'BUTTON'
  icon: '🚙'
  options: ['プリウス', 'アクア', 'カローラ']
  parentAxis: 'maker'
  order: 2

- axisName: 'パーツカテゴリー'
  axisKey: 'parts_category'
  axisType: 'SELECT'
  displayType: 'DROPDOWN'
  icon: '🔧'
  options: ['エンジン', 'ブレーキ', 'サスペンション', '外装', '内装']
  order: 3
```

### イベントカテゴリーの軸
```yaml
- axisName: 'イベント種類'
  axisKey: 'event_type'
  axisType: 'SELECT'
  displayType: 'BUTTON'
  icon: '🎉'
  options: ['音楽フェス', '車イベント', 'スポーツ大会', '展示会']
  order: 1
```

### デジタルカテゴリーの軸
```yaml
- axisName: 'ジャンル'
  axisKey: 'genre'
  axisType: 'SELECT'
  displayType: 'DROPDOWN'
  icon: '📱'
  options: ['PDF資料', '音楽', '動画', 'アプリ']
  order: 1
```

---

## Playwrightテストコード例

```typescript
import { test, expect } from '@playwright/test';

test.describe('A-008 ナビゲーション軸設定ページ', () => {
  test.beforeEach(async ({ page }) => {
    // 管理者ログイン
    await page.goto('http://localhost:3247/admin/login');
    await page.fill('input[name="email"]', 'admin@ec-platform.local');
    await page.fill('input[name="password"]', 'TestAdmin2025!');
    await page.click('button[type="submit"]');
    await page.waitForURL('http://localhost:3247/admin');
  });

  test('TC-A008-001: ページ表示確認', async ({ page }) => {
    await page.goto('http://localhost:3247/admin/navigation');
    await expect(page).toHaveTitle(/ナビゲーション軸設定/);
    await expect(page.locator('h4')).toContainText('ナビゲーション軸設定');
    await expect(page.locator('[role="combobox"]')).toBeVisible(); // カテゴリー選択
    await expect(page.locator('button:has-text("軸を追加")')).toBeVisible();
  });

  test('TC-A008-002: カテゴリー選択機能', async ({ page }) => {
    await page.goto('http://localhost:3247/admin/navigation');
    await page.click('[role="combobox"]');
    await page.click('text=イベント');
    await expect(page.locator('[role="combobox"]')).toContainText('イベント');
  });

  test('TC-A008-005: 軸追加ダイアログの表示', async ({ page }) => {
    await page.goto('http://localhost:3247/admin/navigation');
    await page.click('button:has-text("軸を追加")');
    await expect(page.locator('[role="dialog"]')).toBeVisible();
    await expect(page.locator('h2')).toContainText('ナビゲーション軸を追加');
  });
});
```

---

## 注意事項

1. **現在の実装状況**: UI実装完了、API未実装
2. **ドラッグ&ドロップ**: 現在は↑↓ボタンのみ、ドラッグ&ドロップは後のPhaseで実装予定
3. **プレビュー機能**: プレースホルダーのみ、実装は後のPhase
4. **保存機能**: 現在はconsole.logのみ、実際の保存はAPI実装後

---

**作成日**: 2026-04-16
**最終更新**: 2026-04-16
**テストケース総数**: 20件
